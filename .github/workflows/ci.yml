name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check src/ tests/

      - name: Run tests
        run: pytest tests/ -q

  security:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        # ".[all]" (= dev + anthropic/openai/mistral extras) so pip-audit also
        # scans the optional network-facing AI SDKs and their transitive deps,
        # not just the core install.
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all]"

      - name: Static security analysis
        run: bandit -r src/ridincligun/ -c pyproject.toml

      - name: Dependency vulnerability check
        run: pip-audit
